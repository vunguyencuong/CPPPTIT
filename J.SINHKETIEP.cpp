#include<bits/stdc++.h>
using namespace std;



int main()
{
    while(true)
    {
        int n,k,s;
        cin>> n >> k >> s;
        if(n == 0 && k = 0 && s == 0) return 0;
        int count = 0;
        int a[10000];
        vector<int> v;
        for(int i = 1; i <= n; i++)
        {
            a[i] = i;
        }
        int i = k;
        while(i)
        {
            if()
        }
    }
}