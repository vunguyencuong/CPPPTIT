#include<bits/stdc++.h>
using namespace std;
typedef long long ll;

/*ll cS(ll n)
{
	if(n < 10) return n;
	ll sum = 0;
	while(n > 0)
	{
		sum += n%10;
		n /= 10;
	}
	return cS(sum);
}
*/
int main()
{
	string s;
	cin>> s;
//	ll len = s.length();
//	int count = 0;
	while( num > 9)
	{
	//	ll sum = 0;
	//	ll num = 0;
		ll len = s.length();
		for(int i = 1; i < len; i++)
		{
			s[i] += s[
		}
	}
	cout<< count << endl;
}

